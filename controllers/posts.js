const Post = require('../models/post')

module.exports = {
  index
}

function index(req, res) {
  // Post.find({})
  //   .then(posts => { res.json(posts) })
  //   .catch(err => { res.json(err) })
}